export default {
    // Discord Bot token (https://discordapp.com/developers/applications/me)
    token: process.env.token || '<Here is the token>',
    // The bot's prefix (what triggers the bot's commands)
    prefix: process.env.prefix || '!',
}






