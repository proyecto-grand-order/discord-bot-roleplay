/**
 * This function is called when the bot is ready.
 * @param {import('discord.js').Client} bot The bot client
 */
export default function(bot) {
	console.log(`[!] Logged in as ${bot.user.tag}!`);
}